server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://rm-bp1750425ua9gt560go.mysql.rds.aliyuncs.com:3306/szml?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: szml
    password: szml@2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: 119.91.52.251
      port: 6379
      password: www.2005
      database: 0

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
      id-type: assign_id

# JWT 配置
jwt:
  # 当前密钥
  current-secret: miaoyu-ticket-service-jwt-secret-key-2026
  # 旧密钥
  old-secret:
  # 签发者
  issuer: miaoyu
  # 用户 Token 有效期:24 小时
  user-expiration: 86400
  # 管理员 Token 有效期:8 小时
  admin-expiration: 28800

# 密码学配置
crypto:
  # AES-256 Base64 密钥（32 字节）
  aes-base64-key: REPLACE_WITH_GENERATED_KEY

# 登录防爆破配置
login:
  # 最大失败次数
  max-fail-count: 5
  # 锁定时长:15 分钟
  lock-duration: 900
